export enum DiscrepancyFlag {
  MATCH    = 'MATCH',
  MISMATCH = 'MISMATCH',
  MISSING  = 'MISSING'
}

export enum OfficerDecision {
  PENDING  = 'PENDING',
  ACCEPTED = 'ACCEPTED',
  EDITED   = 'EDITED',
  REJECTED = 'REJECTED'
}

export enum ExtractionStatus {
  SUCCESS = 'SUCCESS',
  PARTIAL = 'PARTIAL',
  FAILED  = 'FAILED'
}